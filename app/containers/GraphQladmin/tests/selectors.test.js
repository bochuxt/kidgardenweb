// import { fromJS } from 'immutable';
// import { makeSelectGraphQladminDomain } from '../selectors';

// const selector = makeSelectGraphQladminDomain();

describe('makeSelectGraphQladminDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
